import type { SiteConfig } from "@/types";

export const siteConfig: any = {
  name: "BDFZ Birthday Checker",
  description:
    "Who's birthday is the same as yours? Check it out!",
  url: "https://birthday.bdfz.app"
};
